# TODO

- [ ] Add global `toggleLike(btn)` in `main/js/base.js`.
- [ ] Wire `toggleLike` to backend endpoint `main/api/users/interactions/toggle-post-like.php` via `fetch`.
- [x] Update UI (icon + liked class + like count) based on JSON response.
- [ ] Manually test: click like on feed page and verify DB table `post_likes` updates.


